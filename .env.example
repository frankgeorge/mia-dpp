# Optional. Without it the Python backend uses deterministic demo mode.
OPENROUTER_API_KEY=sk-or-...
# Models can be changed independently as the workflow grows.
MIA_CONVERSATION_MODEL=deepseek/deepseek-v3.2
MIA_SEMANTIC_MODEL=deepseek/deepseek-v3.2
